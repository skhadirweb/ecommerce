{% extends 'base.html.twig' %}

{% block title %}Erreur de paiement{% endblock %}

{% block content %}
    <h2>Erreur de paiement</h2>
    <p>
        Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong><br><br>
        Il semblerait que votre paiement est échoué pour votre commande <strong>n° {{ order.reference }}</strong><br>
        Une confirmation d'erreur vient de vous être envoyé par mail à l'adresse <strong>{{ order.user.email }}</strong><br>
    </p>
    <hr>
    <a href="{{ path('order') }}" class="btn btn-success">Réessayez le paiement</a>.
{% endblock %}
